import { AppShell } from "@/components/layout/appShell";
import { Badge, Card, PageTitle } from "@/components/ui";
export default function Page(){return <AppShell role="portal"><div className="space-y-6"><PageTitle eyebrow="Client portal" title="One place for buyers, members, students, and booked clients." text="After someone buys, books, joins, or enrolls, they should not be lost in email. They get a portal for access, bookings, support, community, and next steps."/><div className="grid gap-4 md:grid-cols-4">{["Purchased products","Upcoming calls","Course progress","Membership posts"].map((x,i)=><Card key={x} className="p-5"><Badge tone={i%2?"green":"violet"}>Access</Badge><h2 className="mt-4 font-black">{x}</h2><p className="mt-2 text-sm text-slate-600">View, download, reschedule, ask support, or continue learning.</p></Card>)}</div></div></AppShell>}
