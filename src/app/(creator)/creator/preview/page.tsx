import { AppShell } from "@/components/layout/appShell";
import { PublicPreview } from "@/features/bioBuilder/components/bioBuilder";
import { Card, PageTitle, Badge } from "@/components/ui";
export default function Page(){return <AppShell role="creator"><div className="space-y-6"><PageTitle eyebrow="Public preview studio" title="Preview every visitor experience before publishing." text="Creators can preview mobile, desktop, buyer, member, brand, and returning-customer versions of the page."/><div className="grid gap-6 xl:grid-cols-[1fr_440px]"><Card className="p-5"><h2 className="font-black">Preview states</h2><div className="mt-4 grid gap-3 md:grid-cols-2">{["Anonymous visitor","Returning buyer","Paid member","Brand company","Mobile Instagram click","Desktop SEO visitor"].map(x=><div key={x} className="rounded-2xl bg-slate-50 p-4"><Badge tone="violet">State</Badge><p className="mt-3 font-black">{x}</p><p className="mt-1 text-sm text-slate-500">AI can personalize ordering, CTA, language, and offers for this visitor context.</p></div>)}</div></Card><PublicPreview/></div></div></AppShell>}
