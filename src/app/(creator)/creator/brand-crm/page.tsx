import { AppShell } from "@/components/layout/appShell";
import { Badge, Card, PageTitle } from "@/components/ui";
import { brandDeals } from "@/shared/mock/data";
import { FileText, MessageCircle, Sparkles } from "lucide-react";
export default function Page(){return <AppShell role="creator"><div className="space-y-6"><PageTitle eyebrow="Brand CRM" title="Media kits, inbound brand leads, proposals, deliverables, contracts, and reports." text="KreatorOS gives creators a brand-business workflow: AI rate card, proposal builder, collaboration room, campaign short links, invoice/payment status, and deliverable tracking."/><div className="grid gap-6 xl:grid-cols-[1fr_400px]"><Card className="p-5"><h2 className="mb-4 font-black">Deal pipeline</h2>{brandDeals.map(d=><div key={d.brand} className="flex items-center justify-between border-t border-slate-100 py-4"><div><p className="font-black">{d.brand}</p><p className="text-sm text-slate-500">{d.stage} · {d.value} · due {d.due}</p></div><Badge tone={d.stage.includes("Negotiating")?"violet":d.stage.includes("Proposal")?"blue":"amber"}>{d.risk}</Badge></div>)}</Card><Card className="p-5"><h2 className="font-black">AI brand agent</h2><div className="mt-4 space-y-3"><div className="rounded-2xl bg-slate-50 p-4"><MessageCircle className="mb-2 h-5 w-5 text-violet-600"/><p className="text-sm font-bold">Draft reply to ClipSpark</p></div><div className="rounded-2xl bg-slate-50 p-4"><FileText className="mb-2 h-5 w-5 text-violet-600"/><p className="text-sm font-bold">Generate proposal + usage rights</p></div><div className="rounded-2xl bg-violet-50 p-4 text-violet-900"><Sparkles className="mb-2 h-5 w-5"/><p className="text-sm font-bold">Suggest higher package because brand budget is above average.</p></div></div></Card></div></div></AppShell>}
