import { AppShell } from "@/components/layout/appShell";
import { Badge, Card, Metric, PageTitle } from "@/components/ui";
import { stats } from "@/shared/mock/data";
export default function Page(){return <AppShell role="creator"><div className="space-y-6"><PageTitle eyebrow="Analytics" title="AI explains the business, not only charts." text="Track views, clicks, sales, bookings, lead magnets, member churn, brand deal cycle time, provider failures, and workflow ROI."/><div className="grid gap-4 md:grid-cols-4">{stats.map(s=><Metric key={s.label} {...s}/>)}</div><Card className="p-5"><h2 className="font-black">AI insights</h2><div className="mt-4 grid gap-4 md:grid-cols-3">{["Booking clicks are high, checkout is weak. Add proof and lower-priced intro option.","Brand leads have higher value than product sales. Move brand inquiry above course block for brand visitors.","Members who attend office hours are 3x less likely to churn. Automate reminders and recap delivery."].map(x=><div key={x} className="rounded-2xl bg-violet-50 p-4 text-sm leading-6 text-violet-900"><Badge tone="violet">Recommended action</Badge><p className="mt-3">{x}</p></div>)}</div></Card></div></AppShell>}
