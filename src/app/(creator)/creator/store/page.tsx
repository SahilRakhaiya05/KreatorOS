import { AppShell } from "@/components/layout/appShell";
import { Badge, Card, PageTitle } from "@/components/ui";
import { products } from "@/shared/mock/data";
import { ShoppingBag, Sparkles } from "lucide-react";
export default function Page(){return <AppShell role="creator"><div className="space-y-6"><PageTitle eyebrow="Store studio" title="Products, bundles, upsells, gated files, courses, and automated delivery." text="The store is not just a grid of products. Each product can trigger fulfillment, customer tagging, review requests, upsells, memberships, and support workflows."/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{products.map(p=><Card key={p.name} className="p-5"><div className="mb-4 flex h-24 items-center justify-center rounded-2xl bg-slate-100"><ShoppingBag className="h-8 w-8 text-violet-600"/></div><Badge tone={p.status === "Live" ? "green" : "amber"}>{p.status}</Badge><h2 className="mt-4 font-black">{p.name}</h2><p className="mt-1 text-sm text-slate-500">{p.type}</p><p className="mt-3 text-2xl font-black">{p.price}</p><p className="mt-2 text-sm text-slate-600">{p.sales} sales · {p.revenue}</p><div className="mt-4 rounded-2xl bg-violet-50 p-3 text-xs leading-5 text-violet-900"><Sparkles className="mb-2 h-4 w-4"/>{p.automation}</div></Card>)}</div></div></AppShell>}
